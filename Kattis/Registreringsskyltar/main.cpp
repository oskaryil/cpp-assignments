#include <iostream>
#include <vector>

using namespace std;

int plate(vector<int> P(6)) {
  vector<
}

int main() {

  int nPlates;

  cout << "Number of plates seen: ";
  cin >> nPlates;



  vector<int> P(6);
  for(int i = 0; i < P.size(); i++) {
    cin >> P[i];
  }

  cout << P[0];


  return 0;
}